# Pre-registration: momentum / phase1-v3
#
# Registered retrospectively, as the benchmark null. This is the one entry
# here whose result is already known, and it is kept so that the tournament
# has something to calibrate against: if the harness cannot reproduce this
# strategy's measured failure, the harness is wrong.

strategy_id: momentum
version: phase1-v3
status: benchmark_null

mechanism: |
  None established. Retained as the null that any new strategy must beat.

prediction: |
  A completed 15m impulse aligned with the 1h and 4h trend continues far
  enough to reach a 3R target before a structure-anchored stop.

falsification: |
  Directional hit rate at or below 50% at every horizon, or expectancy
  negative out-of-sample net of costs.

outcome: falsified
outcome_evidence: research/results/edge-audit-2024-2026/REPORT.md
outcome_summary: |
  115,929 signals. Hit rate 45.6-47.3% at every horizon from 15m to 24h.
  -0.096 R at base costs, 95% CI [-0.135, -0.056], monthly t = -4.60. At
  zero cost, random entry timing (+0.018%) and the inverted signal (+0.021%)
  both beat following it (-0.046%). 0 of 79 walk-forward parameter variants
  positive out-of-sample.

signal_timeframe: 15m
horizons_bars: [96, 192]
exit_policies: [fixed_rr, extended_rr, structure_target]
cost_scenarios: [frictionless, optimistic, base, realistic_alt, stress]

# Parameterizations
# Registered 2026-07-30. Unlike the other hypotheses, momentum's axis has
# already been explored: 79 walk-forward variants, 0 positive out-of-sample,
# recorded in results/edge-audit-2024-2026. These three settings are not that
# search repeated - they are the benchmark's reproduction check widened from one
# point to three, so a harness fault that happens to flatter the shipped
# configuration cannot hide behind a single number.
#
# The registered point tracks config.yaml, so it moves when the shipped
# configuration moves. The two alternatives bracket it deliberately.
settings:
  - id: registered
    params: {}
    claim: >-
      The shipped configuration, sourced from config.yaml. The arm
      benchmark_check compares against its stored expectation.
  - id: target_2r
    params:
      fixed_reward_risk: 2.0
    claim: >-
      The pre-3R shipped target. Reproduces the earlier audit's parameterisation
      so a change in the harness shows up as a change against it.
  - id: wider_stop
    params:
      min_stop_atr_multiple: 1.5
    claim: >-
      A 1.5 ATR stop floor, the widest setting the shadow registry carries. A
      benchmark null that turns positive here would mean the audit's stop-width
      conclusion no longer reproduces.

hypotheses_tested: 82
multiple_testing_note: |
  79 pre-registered parameter variants scored on a purged walk-forward
  split. Share positive out-of-sample: 0.0%. There is no winner to discount.
