# Archived demo order-path succession record

> **ARCHIVED / FROZEN — not a current checklist or authorization.** This is the
> decision record for replacing rejected momentum on the demo order path.

`momentum` lost 8.97% in the recorded 2026-07-29 through 2026-08-05 demo
window and remained the only mechanism with a statistically clear result—and
that result was negative. The compared alternatives were unproven. The
48-hour, realtime-eligible `ls-ratio-fade` contract was selected for
deterministic demo operation by elimination because its horizon matched the
runtime hold limit and it did not require an analyst prompt.

This was not a tier promotion or profitable-edge claim. Current shipped values
come from `config.yaml` and `agent/registry.py`; current authority and human
approval boundaries are in
[`../AUTONOMOUS_RESEARCH.md`](../AUTONOMOUS_RESEARCH.md). Git history retains
the original episode table and pre-commitment details.
