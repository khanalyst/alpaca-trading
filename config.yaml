# Lean Alpaca runtime. Paper trading is fail-closed by default.
mode: paper

broker:
  paper: true
  data_feed: iex                 # iex | sip | delayed_sip
  options_feed: indicative

session:
  timezone: America/New_York
  entries_regular_session_only: true
  allow_exits_outside_session: true
  force_flat_minutes_before_close: 10
  reject_new_entries_minutes_before_close: 5

universe:
  symbols: [SPY, QQQ, IWM, DIA]
  asset_classes: [us_equity, us_option]
  min_price: 1.0
  max_symbols: 50
  denylist: []

strategy:
  id: ibr
  version: v1
  range_minutes: 15
  breakout_buffer_bps: 5
  min_relative_volume: 1.0
  target_r: 2.0
  max_entry_extension_r: 1.0
  min_ibr_width_atr: 0.25
  max_ibr_width_atr: 3.0
  latest_entry_time: "15:00"
  force_flat_minutes_before_close: 10

risk:
  risk_per_trade_pct: 0.5
  daily_loss_limit_pct: 2.0
  max_open_risk_pct: 2.0
  max_concurrent_positions: 3
  max_position_notional_pct: 25.0
  options_min_dte: 7
  options_max_dte: 60
  options_max_spread_pct: 10.0

execution:
  order_type: market
  time_in_force: day
  client_order_id_prefix: ibr
  max_slippage_bps: 50

llm:
  provider: openai
  model: gpt-5.6-sol-coding
  temperature: 0.2
  max_tokens: 2000
